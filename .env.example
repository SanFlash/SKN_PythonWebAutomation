# Darshan Hotel — Playwright + Pytest + Allure Automation Framework
# Author: Satyendra Kumar Namdeo
#
# Copy to `.env` and adjust as needed. Every value here is optional —
# anything left unset falls back to config/environments.yaml.

APP_ENV=dev

# BASE_URL=
# DEFAULT_TIMEOUT=
# DEFAULT_NAVIGATION_TIMEOUT=
# VIEWPORT_WIDTH=
# VIEWPORT_HEIGHT=
